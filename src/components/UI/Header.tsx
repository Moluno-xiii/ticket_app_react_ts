const Header: React.FC = () => {
  return (
    <header className="border-dark border-2 rounded-lg flex flex-row justify-between items-center">
      Header component
    </header>
  );
};

export default Header;
